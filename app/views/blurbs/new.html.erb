<div class="row">
  <div class="col-md-6 col-md-offset-3 well">
    <div class="signform">
      <h2>
        <i class="fa fa-comment-o"></i> Create new blurb
      </h2>
    </div>
    <%= form_for(@blurb) do |f| %>
      <%= render 'form', f: f %>
      <button type="submit" 
              class="btn btn-primary">
           <i class="fa fa-save"></i> 
            Create blurb
      </button>
    <% end %>
  </div>
</div>